extends Layout/base
include mixins/videoBlock

block content
    .search-header
        h3 Searching for: #{searchingBy}
    .search-videos
        each item in videos
            +videoBlock({
                title: item.title,
                views: item.views,
                videoFile: item.videoFile
            })